package com.example.musica_be.repository.settlement;

import org.springframework.data.jpa.repository.JpaRepository;

public interface SettlementStatus extends JpaRepository<SettlementStatus, Integer> {
}
